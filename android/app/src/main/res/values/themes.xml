<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real theming (see ui/theme/Theme.kt); this is just the
         pre-Compose window background/splash so cold start doesn't flash white. -->
    <style name="Theme.Hodora" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Live position for turn-by-turn navigation and route planning. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Phase 3/4: lets NavigationService/RecordingService keep reporting
         position, updating their ongoing notification, and (nav only)
         speaking turn cues once the rider backgrounds the app or locks the
         screen — the whole reason a native app exists instead of the
         Capacitor WebView shell, which mobile browsers throttle into
         uselessness the moment the tab is hidden. ACCESS_BACKGROUND_LOCATION
         must be requested at runtime as a separate step *after* foreground
         location is already granted (Android 10+); see
         ui/permissions/BackgroundLocationChecklist.kt. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Lets the rider exempt Hodora from OEM battery managers that would
         otherwise kill NavigationService/RecordingService mid-ride —
         requested optionally, with an explanation, never silently. See
         ui/permissions/BackgroundLocationChecklist.kt. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".HodoraApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".nav.NavigationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".record.RecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
